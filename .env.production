# ============================================================
# 프로덕션 배포 전용 — Git에 추적됨 (비밀값 금지)
# ============================================================
# Vercel이 프로덕션 빌드 시 이 파일을 자동으로 읽습니다.
# Vercel 대시보드에 같은 키가 있으면 대시보드 값이 우선 적용됩니다.
# ============================================================

# BE 배포 서버 주소 (HTTPS 전용)
VITE_API_URL=https://ddotg.dev
